# ===== pnpm 配置 =====

# 允许宽容的 peer dep 解析（eslint v9 等的 peer 不完全匹配也允许装）
auto-install-peers=true
strict-peer-dependencies=false

# 跳过提示，CI 上更清爽
audit=false
fund=false

# 兼容性：让某些只看 node_modules/<pkg> 而不会去 .pnpm 软链下找的旧工具能找到包
# 老 webpack 插件 / 工具有时会要求扁平结构，开 shamefully-hoist 兜底
shamefully-hoist=true
