# 允许宽容的 peer dep 解析
legacy-peer-deps=true

# 网络超时：单次请求最多 60s（不要太长，否则卡死无感知）
# 重试 3 次：覆盖大多数瞬时抖动
fetch-retries=3
fetch-retry-mintimeout=5000
fetch-retry-maxtimeout=20000
fetch-timeout=60000

# 跳过 audit 和 fund 提示，省时间也减少内部代码路径
audit=false
fund=false
